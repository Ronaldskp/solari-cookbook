# Solari API key — never commit the real value.
# Get a key at https://console.getsolari.com
# Either export it in your shell, or copy this file to `.env` and fill it in.
SOLARI_API_KEY=slr_live_xxxxxxxxxxxxxxxxxxxxxxxx
